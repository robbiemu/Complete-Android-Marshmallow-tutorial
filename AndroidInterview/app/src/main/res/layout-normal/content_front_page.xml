<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/lg"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="xyz.selfenrichment.robertotomas.androidinterview.FrontPage"
    tools:showIn="@layout/activity_front_page">

    <Button android:id="@+id/third_button"
        android:text="@string/third_button"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_states"
        android:textSize="20sp" android:textColor="#ffffff" />

    <Button
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:text="@string/second_button"
        android:id="@+id/second_button"
        android:layout_above="@id/third_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:background="@drawable/button_states"
        android:textSize="20sp" android:textColor="#ffffff" />

    <Button
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:text="@string/top_button"
        android:id="@+id/top_button"
        android:layout_above="@id/second_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:background="@drawable/button_states"
        android:textSize="20sp" android:textColor="#ffffffff" />

    <Button
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:text="@string/bottom_button"
        android:id="@+id/bottom_button"
        android:layout_below="@id/third_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:background="@drawable/button_states"
        android:textSize="20sp" android:textColor="#ffffff" />
</RelativeLayout>
